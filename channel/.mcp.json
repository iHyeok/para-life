{
  "mcpServers": {
    "para-life-channel": {
      "command": "bun",
      "args": ["run", "--cwd", "${CLAUDE_PLUGIN_ROOT}", "--shell=bun", "--silent", "start"],
      "env": {
        "PARA_CHANNEL_PORT": "3847",
        "SKIP_AUTH": "1"
      }
    }
  }
}
